// Defines the main function. 
function sum(){

    // Variables 
    const x = 5; 
    const y = 9; 

    // Creates a sum.
    const sum = x + y; 

    // Updates the index document.
    document.getElementById('output').textContent = "The sum: " + sum; 
}

// Runs the main function. 
sum; 